import {SxProps, Theme} from "@mui/material";

const listStyle: SxProps<Theme> = {
    backgroundColor: "primary.main",
    borderRadius: 2,
    border:"1px solid",
    borderColor:"white"
}

export {listStyle}